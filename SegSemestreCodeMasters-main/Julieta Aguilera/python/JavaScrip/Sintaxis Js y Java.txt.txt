Sintaxis similares entre Java y Javascript
Operadores Simplificados/ compuestos:
+=
-=
*=
/=
%==
**=

Operadores Aritmeticos 
+
-
*
/
%   //Residuo
**  //Exponente

Operadores de Asignacion
=
Loa operadores simplificados tambien se asignan 

Operadores de comparacion (Boolean)
== Revisa el valor sin importar el tipo de dato
=== Revisa el valo y tambien el tipo

Documentar 
// = Para lineas de comentario 
/* */ para multiples lineas de comentario

Secuencias de escape 
\' = Mostrar una comilla simple dentro de una cadena en la salida de informacion
\" = Mostrar doble comilla dentro de una cadena en la salida de informacion  
\\ = Barra invertida, diagonal inversa, es para mostrar un back slash
\r = Retorno
\t = Tabulacion
\b = Retroceso
\f = Salto de linea

Operador de aumento
++
Ejemplo: let z = ++a; //PreIncrmento
Ejemplo: z = a++; //PosIncrmento

Operador de Decremento
--
Ejemplo: let z = ++a; //PreDecrmento
Ejemplo: z = a++; //PosDecrmento

Operador distinto, diferente(Boolean)
!= //comprueba valores sin revisar el tipo
!== compruba el valor y tambien el tipo

Operadores Relacionales (Boolean)
<
>
<=
>=

Operadores logicos 
And -> && Doble aspersan
Or  ->
